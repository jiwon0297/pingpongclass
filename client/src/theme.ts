import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#dd7e8f',
    },
    secondary: {
      main: '#7c99c6',
    },
    warning: {
      main: '#ffffff',
    },
  },
});

export default theme;
