import React from 'react';
import LoadingImg from '../assets/images/loadingimg.gif';

const Loading = () => {
  return (
    <div>
      <img src={LoadingImg} />
    </div>
  );
};

export default Loading;
